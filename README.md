# Truecaller-backend

# Node-Backend-Base

Creating a Backend base so that i can start any new project without any hassle for creating the base.

# Base URL

URL = "http://localhost:3000/swagger/"

# Important Commands

to start the server = npm run start
to build the project = npm run build
to start the server using nodemon = npm run dev
to generate routes = npm run generate-routes
